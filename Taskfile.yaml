version: '3'

tasks:
  test:
    cmds:
      - ./scripts/test.sh
  build:
    dotenv:
      - './.env'
    cmds:
      - GITHUB_SHA="$(cat /dev/urandom | tr -dc 'a-f0-9' | fold -w 6 | head -n 1)" ./scripts/publish.sh
